const { EmptyUpgradeScript } = require('@companion-module/base')

module.exports = [
	// v0 — placeholder
	EmptyUpgradeScript,
	// v1 — placeholder (password stays in config, secret-text type masks it in UI)
	EmptyUpgradeScript,
]
